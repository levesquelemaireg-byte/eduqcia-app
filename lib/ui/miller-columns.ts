/**
 * Miller columns (CD, connaissances) — listes sans scroll interne.
 * Voir `docs/WORKFLOWS.md` (TAE stepper — Miller columns).
 * Layout conteneur : `app/globals.css` (`.tae-miller-host`, `.tae-miller-grid--3|4`, `.tae-miller-col`).
 */
export const MILLER_COLUMN_LIST_CLASSNAME = "p-2 text-left";

/** Boutons de choix : longues étiquettes sans débordement horizontal ; alignement gauche (les `<button>` ont `text-align: center` par défaut navigateur). */
export const MILLER_CHOICE_BTN_WRAP = "min-w-0 max-w-full whitespace-normal break-words text-left";
